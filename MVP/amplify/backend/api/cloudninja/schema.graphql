type CategoryModel @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  message: String
}

type ProductModel @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  message: String
}

type EventModel @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  message: String
}
 